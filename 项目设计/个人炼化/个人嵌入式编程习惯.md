# 一、全局编程习惯
对于新添加的代码，需要遵循以下规则：
1.  括号采用K&R 风格。
2. 函数命名采用英文功能描述风格，单词中间采用`_`连接。名称可以长，不能为了精简降低可读性。例如：LED_Task_Start();
3. 如果需要添加新文件，同时添加.c/.h文件。.c文件中需要包含该.h文件。
4. .h文件的内容：声明.c文件中的函数，声明结构体，声明extern变量。
5. 
# 二、如果使用freertos
1. API函数使用freertos提供的函数，不使用cmsis提供的API函数。
2. 