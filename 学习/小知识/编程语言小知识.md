## 消除参数未使用警告
在回调函数中，例如信号处理、定时器回调、线程入口，经常会固定回调原型，结构上要求必须有参数。如果在实际的函数处理中没有用到，放任不管，编译的时候会有参数未使用警告。
解决方案：void强制转换。
```c
void test(int a, int b) { (void)a; (void)b; }
```
## memset函数
函数结构：memset(起始地址, 填充值, 字节长度)
作用：清空指定栈上内存。
```c
实例：
memset(handle, 0, sizeof(Button));
Button为链表结构体，handle作为指针指向首地址
```